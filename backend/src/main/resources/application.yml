server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db_LexiLoop_core?useSSL=false&allowPublicKeyRetrieval=true&characterEncoding=utf8&serverTimezone=Asia/Ho_Chi_Minh
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

app:
  jwt-secret: LexiLoopHqtcsdlSecretKeyMustBeLongEnough2026!!
  jwt-expiration-ms: 86400000
